# Example Environment Variables Configuration
# This shows how to properly configure the application for production
# Copy this to .env and fill in your actual values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL connection settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dataset_platform
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# Connection pool settings (optional)
DB_MIN_CONNECTIONS=2
DB_MAX_CONNECTIONS=10

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# JWT Secret Key - MUST be changed in production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# JWT Algorithm
ALGORITHM=HS256

# Token expiration time (in minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Password hashing rounds (higher = more secure but slower)
# Default bcrypt rounds is 12, can increase for production
# BCRYPT_ROUNDS=12

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Application environment
ENVIRONMENT=development  # Options: development, staging, production

# Server settings
APP_HOST=0.0.0.0
APP_PORT=8000

# Debug mode (set to False in production)
DEBUG=True

# Logging level
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Allowed origins for CORS (comma-separated)
# In production, specify exact origins instead of "*"
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# =============================================================================
# OPTIONAL: PRODUCTION ENHANCEMENTS
# =============================================================================

# Rate limiting (requests per minute)
# RATE_LIMIT_PER_MINUTE=60

# File upload settings
# MAX_UPLOAD_SIZE_MB=50
# UPLOAD_DIR=/var/uploads

# Email settings (for password reset, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-email-password
# SMTP_FROM=noreply@yourdomain.com

# Redis for caching (optional)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=

# Sentry for error tracking (optional)
# SENTRY_DSN=https://your-sentry-dsn

# =============================================================================
# NOTES
# =============================================================================

# 1. NEVER commit .env file to version control
# 2. Add .env to .gitignore
# 3. Use different .env files for dev/staging/prod
# 4. Rotate SECRET_KEY periodically in production
# 5. Use strong passwords for database
# 6. Enable SSL/TLS for database connections in production
# 7. Consider using a secrets manager (AWS Secrets Manager, HashiCorp Vault)

# =============================================================================
# USAGE IN CODE
# =============================================================================

# To use these in your Python code:
#
# from dotenv import load_dotenv
# import os
#
# load_dotenv()
#
# DB_HOST = os.getenv("DB_HOST", "localhost")
# SECRET_KEY = os.getenv("SECRET_KEY")
#
# Never use hard-coded values in production!
